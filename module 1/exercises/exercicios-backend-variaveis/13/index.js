// declaration of variables

const radioCylinder = 3;
const hightCylinder = 1;

// total area of the cylinder

let cylinderArea = 2 * radioCylinder * (radioCylinder + hightCylinder);

console.log(`the total area of the cylinder is ≅ ${cylinderArea}π`);
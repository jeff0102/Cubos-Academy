const nomes = ["Ana", "Joana", "Carlos", "amanda"];

// meu codigo aqui 

let namesWithAs = [];

for (const name of nomes) {
    if (name[0] == 'a' || name[0] == 'A') {
        namesWithAs.push(name);
    }
    
}
console.log(namesWithAs);

// i liked to solve this exercise, was very funny
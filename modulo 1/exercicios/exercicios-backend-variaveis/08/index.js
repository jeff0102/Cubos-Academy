// declaration of variables

let pi = Math.PI;
let radius = 1;

let length = 2 * pi * radius;
length = length.toFixed(2);

let area = pi * radius ** 2;
area = area.toFixed(2);

console.log (`o comprimento é ${length} e a area é ${area}`);